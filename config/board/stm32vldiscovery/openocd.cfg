init

proc gcc_arm_flash {IMAGE} {
	reset halt
	sleep 100
	wait_halt 2
	flash write_image erase $IMAGE
	sleep 100
	verify_image $IMAGE
	sleep 100
	reset run
}

proc gcc_arm_erase {} {
	reset halt
	sleep 100
	stm32f1x mass_erase 0
	sleep 100
}
